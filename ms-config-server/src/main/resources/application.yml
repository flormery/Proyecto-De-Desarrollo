server:
  port: 7070




spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/flormery/Proyecto-De-Desarrollo.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main

  security:
    user:
      password: 123456
      name: root